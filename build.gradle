apply plugin: 'java'
apply plugin: 'maven'

group="org.xwidgets"
version="1.0.0-SNAPSHOT"

uploadArchives {
   repositories.mavenDeployer {
      repository(url: "file://~/.m2/repository/")
      pom.artifactId = 'xwidgets'
   }
}
